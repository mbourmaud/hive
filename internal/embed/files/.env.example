# ===========================================
# Hive Multi-Agent Configuration
# Copy this to .env and fill in your values
# ===========================================

# Git User (required)
GIT_USER_EMAIL=your.email@example.com
GIT_USER_NAME=Your Name

# Workspace (required)
WORKSPACE_NAME=my-project
# Optional: Auto-clone a git repo on first start
# GIT_REPO_URL=https://github.com/user/repo.git

# =============================================
# Claude Authentication
# =============================================

# For Interactive Mode (Queen, manual workers):
#   - Only CLAUDE_CODE_OAUTH_TOKEN required
#   - Get from: claude setup-token
#   - Works with Claude Max/Pro subscription
CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# For Autonomous Workers (WORKER_N_MODE=daemon):
#   Daemon auto-detects which backend to use based on available credentials.
#   Priority: API Key > Bedrock > CLI
#
#   Option 1: Anthropic API Key (pay-as-you-go, NOT included in Claude Max/Pro)
#     Get from: https://console.anthropic.com/settings/keys
#     Format: sk-ant-api03-...
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
#
#   Option 2: Claude CLI with OAuth (FREE with Claude Max/Pro)
#     Uses CLAUDE_CODE_OAUTH_TOKEN above
#     Daemon calls 'claude' CLI in subprocess
#     No additional setup needed!
#
#   Option 3: AWS Bedrock (enterprise)
AWS_PROFILE=bedrock
AWS_REGION=us-east-1
# CLAUDE_CODE_USE_BEDROCK=1

# Advanced: Force specific backend (auto-detected if not set)
# Options: api (API key), cli (Claude CLI), bedrock (AWS Bedrock)
# HIVE_CLAUDE_BACKEND=cli

# =============================================
# Claude Models (optional)
# =============================================
QUEEN_MODEL=opus
WORKER_MODEL=sonnet

# =============================================
# Docker Image Selection (optional)
# =============================================
# Choose which Dockerfile to use for agents
# Options: docker/Dockerfile.node (default)
#          docker/Dockerfile.minimal
#          docker/Dockerfile.golang
#          docker/Dockerfile.python
#          docker/Dockerfile.rust
# HIVE_DOCKERFILE=docker/Dockerfile.node

# =============================================
# Version Control System (optional)
# =============================================

# GitHub
GITHUB_TOKEN=your_github_token_here

# GitLab
# GITLAB_TOKEN=your_gitlab_token_here
# GITLAB_HOST=gitlab.com

# =============================================
# Paths (optional, defaults shown)
# =============================================
# HIVE_WORKSPACES=./workspaces
# HIVE_TASKS=./tasks

# =============================================
# Worker Modes (optional)
# =============================================
# Choose mode for each worker: interactive (default) or daemon
# - interactive: Manual CLI mode, you control the worker
# - daemon: Autonomous mode, worker polls Redis and executes tasks automatically
#
# WORKER_1_MODE=interactive
# WORKER_2_MODE=daemon
# WORKER_3_MODE=interactive
# WORKER_4_MODE=interactive
# WORKER_5_MODE=interactive
# WORKER_6_MODE=interactive
# WORKER_7_MODE=interactive
# WORKER_8_MODE=interactive
# WORKER_9_MODE=interactive
# WORKER_10_MODE=interactive

# =============================================
# Misc (optional)
# =============================================
# Auto-install dependencies on first start
AUTO_INSTALL_DEPS=true

# Log timestamp format (strftime format)
# Examples:
#   %d-%m-%Y %H:%M  →  22-12-2025 20:30
#   %Y-%m-%d %H:%M  →  2025-12-22 20:30
LOG_TIMESTAMP_FORMAT=%x %H:%M

# Node.js max memory (in MB, default: 4096 = 4GB)
# Increase if you get "JavaScript heap out of memory" errors
NODE_MAX_OLD_SPACE_SIZE=4096
